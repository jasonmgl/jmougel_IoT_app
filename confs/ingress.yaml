apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app
  namespace: dev
spec:
  ingressClassName: traefik
  rules:
  - host: jmougel.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app
            port:
              number: 8888